# Lessons

- When adding or updating npm libraries, save exact installed versions only. Do not use `^`, `~`, or `*` ranges in package manifests; keep `package-lock.json` in sync with the exact versions.
