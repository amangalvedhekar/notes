import { Meta } from '@storybook/addon-docs/blocks';
import { componentCatalog } from '@notes/components';

<Meta title="Getting Started/Overview" />

# Components Documentation

This Storybook documents the UI components in `@notes/components`. These components are wrappers around
[Tamagui](https://tamagui.dev/).



## Available Components

### Atoms

<ul>
  {componentCatalog.atoms.map((componentName) => (
    <li key={componentName}>
      <code>{componentName}</code>
    </li>
  ))}
</ul>

### Molecules

<ul>
  {componentCatalog.molecules.map((componentName) => (
    <li key={componentName}>
      <code>{componentName}</code>
    </li>
  ))}
</ul>

### Templates

<ul>
  {componentCatalog.templates.map((componentName) => (
    <li key={componentName}>
      <code>{componentName}</code>
    </li>
  ))}
</ul>

### Icons

<ul>
  {componentCatalog.icons.map((componentName) => (
    <li key={componentName}>
      <code>{componentName}</code>
    </li>
  ))}
</ul>
